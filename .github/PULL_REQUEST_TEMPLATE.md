## Summary

<!-- What changed, and why it matters to someone reviewing this cold. -->

## Type of change

- [ ] New example (`examples/`)
- [ ] Tool integration (`tools/`)
- [ ] Spec / template change (`SPEC.md`, `agdr-template.md`)
- [ ] CI / tooling (`.github/workflows/`, `scripts/`)
- [ ] Docs (`README.md`, `CONTRIBUTING.md`, etc.)
- [ ] Other

## Checklist

- [ ] `npm run validate` passes (if you touched `examples/`, `schema/`, or `scripts/validate-agdr.js`)
- [ ] `npm run validate:changelog` passes, or `CHANGELOG.md` has a new entry matching the manifest version bump (if you touched `.claude-plugin/plugin.json`)
- [ ] Markdown links resolve (CI runs this on every PR; check `lychee` output if it fails)
- [ ] If this changes a normative rule, [SPEC.md](../SPEC.md) is updated (not just the template or README)

Closes #
